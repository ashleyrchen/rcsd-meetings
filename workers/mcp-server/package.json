{
  "name": "rcsd-mcp-server",
  "version": "1.0.0",
  "description": "RCSD Open Data MCP Server — query Redwood City School District data",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.mjs",
    "test:local": "node test.mjs http://localhost:8787"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260510.1",
    "wrangler": "^4.90.0"
  },
  "//overrides": "ws: miniflare (via wrangler) pins ws@8.18.0 exactly, vulnerable to GHSA-58qx-3vcg-4xpx (fixed in 8.20.1). Remove once wrangler ships a miniflare with ws >= 8.20.1.",
  "overrides": {
    "ws": "^8.20.1"
  }
}
